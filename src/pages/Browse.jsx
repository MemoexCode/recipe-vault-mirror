import React from 'react';

// This is a temporary, minimal version of the Browse page.
// Its only purpose is to test if the main layout and routing are working.
// It has no dependencies and cannot crash.

export function Browse() {
  return (
    <div>
      <h1 className="text-2xl font-bold">Diagnosetest</h1>
      <p className="mt-2">Wenn Sie diesen Text sehen, funktioniert das Haupt-Layout korrekt.</p>
    </div>
  );
}